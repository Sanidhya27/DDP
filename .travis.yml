dist: bionic
sudo: true
language: c++
compiler:
  - clang
addons:
  apt:
    packages:
      - libitpp-dev
      - libgtest-dev
      - cmake
script:
  - cd /usr/src/gtest && sudo cmake CMakeLists.txt && sudo make && sudo cp *.a /usr/lib && sudo ln -s /usr/lib/libgtest.a /usr/local/lib/gtest/libgtest.a && sudo ln -s /usr/lib/libgtest_main.a /usr/local/lib/gtest/libgtest_main.a && cd -
  - cd givens_params && make && make test
